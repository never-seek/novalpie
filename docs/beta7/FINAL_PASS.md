# Beta7 集中收尾顺序

用户2026-09-09要求加快且不漏项。本表安排后续工作；不取代requirements.md的96项及各专项证据，不凭此简表缩小范围。

## 1. 优化包与保留数据

- 当前唯一R8候选：8900b7de...98fa4f3，code2026090901/3,227,047bytes/原Beta6证书。实际Beta6→候选install-r已成功。
- 旧JUnitRunner缺Trace为测试驱动问题；纯JavaSDK runner已运行。当前差异是最初快照将/data/data canonical文件相对于/data/user/0别名计算，产生`../../../../data/<package>/files/...`。仅归一化该精确同包别名，再逐文件比对SHA/长度，绝不跳过缺项。
- 修测试后完成原快照verifyOnly，不重跑降级、不卸载。随后普通启动并实际浏览收藏/搜索/详情/正文/论坛/个人，捕获R8异常；这一步发现的生产崩溃为阻断。
- 当前项目Gradle依赖缓存已缺失；在线恢复只下载同版本项目依赖，不升级库或改机器配置。20:57开始46535，只一个Gradle，MuMushutdown。

## 2. 同包核心验收

- 以SDK-only驱动/普通UI操作验证优化包；旧Debug辅助活动不计入R8通过。必要时把验证逻辑置于测试APK，不向成品添加debug入口或放宽优化。
- 阅读：目录/菜单/反向尾页/连续章评与底栏/音量开关/重排锚点；已被工具策略拦截的360661深链操作不绕行，记录特定样本缺口。
- TTS：可用中文系统引擎实际发声、连续首句、通知/锁屏、焦点/耳机、禁用隐藏。规则与原文派生核对。
- 下载：小书原文/规则×TXT/EPUB；图格式/真实出现数；此前12GB全书报告保持来源，最终同包关键管线重新验。源未提供图/章不得伪造。
- 适配：尺寸/字体/明暗/键盘/返回/断网与进程恢复；性能冷/热3轮的同手势、同书、同设备量化。不可把测试总数当性能已通过。

## 3. 缺口实现集中提交

- 上传持久草稿已验证，但大JSON创建首批→续批的完整任务检查点/恢复未完成；原始EPUB及源书/批ID冻结，未知写入不盲重放。
- 继续编辑器剩余状态/远端管理动作与源控件缺口；原生不退化网页，账号/角色/返回/异常回执隔离。
- 每个明确缺陷先有失败用例，相关修订合并一次全量gate，避免改动与编译同时发生。必要协议/危险动作仅受控数据，普通角色实际证据不伪造。

## 发布前唯一门禁

所有requirements关联最终实际包证据、未关闭阻断项为零、源码commit/标签/APK哈希一致。发布前再次刷新反馈及站点build。中文说明保留各板块App自身增强、与网页差异、必要WebView和未认证OEM限制。新建Beta7 Release，Beta6保留。
