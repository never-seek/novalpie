# Beta 7 验证记录与发布门禁

当前：**NOT READY TO RELEASE**。Beta 7 业务代码尚未替换，以下门禁没有完成。

## 记录格式

每份证据至少给出：需求/控件/反馈 ID、源码 commit（或 dirty diff hash）、构建 variant、APK SHA-256、设备 SDK/尺寸/density/fontScale、前置状态、实际操作、可见结果、请求回执/截图路径，以及是否恢复测试状态。

源码命中/字符串 golden 只能证明静态存在；纯单测不能证明按钮可达；TTS 错误提示不能证明实际发声；已装包与构建包不同则不能关闭当前版本验收。

## 门禁

- [ ] B7-01 完整需求/源站/反馈台账，角色与登录态分开，没有未标明的覆盖空洞。
- [ ] 每个实现的 API 合同测试：参数/分页/权限/空和异常响应/取消。
- [ ] 每项修复的先失败回归证据及最终通过结果；不为迁就实现放宽要求。
- [ ] 真实行分页内容覆盖/不重不漏；跨章前后/大图迟到/长段落/字体重排；固定反馈书 360661 EP49/EP50。
- [ ] MuMu 中文 TTS 实际发声、前后台/锁屏/连续/暂停恢复/音频焦点；五种翻页动画及音量键开关。
- [ ] 原文/应用替换 × EPUB/TXT；含动态图和重复引用的样本、小书/大书、1GB+ 图书；原图哈希/引用位置/ZIP/TOC 校验及外部阅读器打开。
- [ ] 覆盖安装 Beta 6→Beta 7，验证全部本地 store 与已有下载不丢；失败迁移可恢复。
- [ ] MuMu 320/360/412/600dp × 字体 1.0/1.3/2.0；明暗、键盘、安全区、返回、断网、进程重建。
- [ ] 冷/热各三轮性能：同设备、同书/同列表/同手势，热纯文本掉帧≤5%，图列表不劣于 Beta 6，主线程无逐像素全章解析、内存不随累积章节持续上涨。
- [ ] 普通账号/管理员实际权限差异与危险操作受控测试；不能用管理员一份截图代替普通用户测试。
- [ ] optimized beta 构建单测/编译/lint/R8 后运行均通过；本地 gate 可离线重现。
- [ ] 发布前源站 build/新反馈增量复查；当前无阻断功能、稳定性或数据完整性缺陷。
- [ ] 标签对应构建 commit，验收 APK 与 GitHub asset digest/证书/包名/版本一致。

## 本轮已获得的基线证据

- 2026-09-05 执行前 Debug gate 返回 0，APK 仍为公开 Beta 6 `30f07ac0...9707d6c3`，不是 Beta 7 验收。
- 公开研究工具 13 项测试通过：GET allowlist、敏感字段最小化、分页缺口、nested provenance、AST 路由/事件/请求、版本化路径、作用域与带哈希验证的 method wrapper 基址。该结果只验证采集工具。
- MuMu 0 正常启动，Android 15，900×1600/240dpi，ADB 127.0.0.1:16384；初始旧包 `1d1dcc...` 已以 `install -r` 覆盖至当前 Beta 6 `30f07ac0...9707d6c3`，设备 SHA-256 一致，reverse tcp:7890 正常。原数据保留，阅读历史、现有继续阅读和四列设置仍在；这不是 Beta 7 升级验收。证据 `20260905-public/mumu-published-base.png` 和 UI XML。
- MuMu 无默认 TTS 引擎（`tts_default_synth=null`），真实发声门禁仍待配置。
- 网站已取得 61 份 360×732 安卓触控页面/子面板结构取证（登录后48、访客13），截图存 `agent-bridge/artifacts/beta7-baseline/20260905-browser/`；其中骨架、无效 query 或重复对照不计为完成行为。28 个 GET 合同有浏览器响应状态关联，响应完整 schema/控件映射与全量写入测试仍待补齐。完整控件覆盖仍在进行，不声明全站已摸清。
