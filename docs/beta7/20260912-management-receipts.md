# 2026-09-12：编辑器索引安全与书籍管理回执

本轮是Beta7剩余缺陷修复，不是全项目通过/发布声明。主机提交内存不足，MuMu仍未安装这些改动；未对真实作品执行转让、删除、改权限或上传。

## 编辑器删除期间打开另一章

- 实际Compose界面测试：A/B/C列表，删除A后异步发布仍未完成，触摸B。原实现打开“编辑第2章”，随后索引可能指向C。
- `EditorDialogPublicationTest`在34153真实断言失败，不是找不到UI节点；章节卡片在`busy`期间禁用且事件入口再校验，处理完成后B能以index0/B正文保存。新增章节的排队行为保留。
- 20121：27tests/4suites、零失败。Hooke独立复核确认原第6项及此索引问题已解决，未给予全项目发布批准。

## 书籍/章节操作误报成功

真实App API + MockWebServer（仅本地合成数据）复现：

1. 元信息保存：外层拒绝被内层data覆盖；空响应当成功；外层失败字段/errors被丢弃。
2. 转让：只有目标ID、没有明确回执，也会当成功。
3. 插图删除：只有图数或内层拒绝，也能走调用方成功分支。
4. 门槛：空回执被当作成功。
5. 章节排序/插入/修改/删除/批删/翻译：12种“空响应或内层拒绝”组合均会返回到成功分支，可能清掉编辑草稿、待保存排序或选择。

七项回归6562全部红。38192修后新增七项通过，但旧测试发现合法部分保存回执不应改变语义；保留`success=true + failedFields`以及完整errors后，95822原133项通过，新增章节组合回归失败（12项错误接受）。六种章节动作改为严格回执处理后，79656完整1203tests/169suites全部通过，0失败/错误/跳过；未修改旧用例的期望掩盖兼容性回归。

`feature/books/ManagedBookMutationResponse.kt`负责领域回执：检查data/result各层，任何明确拒绝立即失败；无明确成功则提示先刷新核对、未自动重发。元信息合法部分成功保留失败字段；其余失败走现有onFailure而不进入清草稿/清选择的成功路径。网络路径、参数、原图字节、签名和会话协议没有改动。

Hooke对本分片静态独立复核未发现剩余正确性问题，确认部分成功与各调用方失败分支仍保留草稿。审查时完整测试尚在运行；之后主线程已确认79656完整绿。此复核仍非整个Beta7的发布批准。

## 剩余边界

61157低内存独立优化打包成功，产物bfeddab2...911af/3,259,834bytes通过签名/包名/版本/非debug/无audit组件核验。原R8预算83160因系统本地内存不足崩溃，已记录，不能掩盖；新包尚未装。独立lint19408通过（0错误5警告），git diff --check通过。17:25可用提交约2.65GiB、ADB为空，未尝试以卸载/清数据解决，尚未发布。

- 以上证明协议与Compose行为，不冒充MuMu真实管理操作通过。
- 书籍/章节管理的独立ViewModel与账号/请求代次、重新加载保留草稿仍是下一分片，不因回执修复而标记完整。
- 最终包的设备矩阵、TTS/下载组合与发布门禁仍保持未完成。
