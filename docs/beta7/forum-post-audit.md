# 论坛详情与评论状态（进行中）

保留现有原生UI与富文本解析入口。新ForumPostRepository/ViewModel负责独立正文、分页评论、书卡、按帖草稿/回复、反应/投票写入与返回scroll；根VM移除约450行旧请求/写入拼装逻辑。

## 新问题和修订

- 旧提交中切换replyToName/commentClientRequestId会导致回包被丢弃、actionLoading不复位。新请求冻结postId/target/content/requestId；完成后释放busy，但只清对应提交草稿，新目标草稿保留。
- 切换帖子/账号后旧回包不可改当前身份；同帖刷新不取消正在发送的写操作。正文与评论可独立读取/重试。
- 原详情只请求100根评论；新增真实分页模型与“加载更多”，不把嵌套回复数当根分页数。刷新保留已加载窗口；22686更严格顺序回归红，修后按原顺序append。
- 返回按帖保存位置；缓存正文六帖，带草稿仍保留（未来可再把大正文和纯草稿拆开）；不把网页投票反应协议改掉。
- 根评论和回复同数字ID是防御性用例，752历史抓取帖未发现实际冲突；改复合身份去重/正确根lookup，避免潜在丢失。不声称线上已复现。

## 验证

41709最初三VM测试通过；40386完整150suites/1069tests/Debug/AndroidTest通过，当前`CB071E6B9A0A7B647C77802F745B5E95D158D819F2ECE73DD83AEF84A3B64254`已无损安装。

78268实机App1871第一步成功但旧公告1422已被源站删除，后续超时；真实页面显示“帖子不存在”，不是App转圈。换当前存在1934，9396重建测试包后20463实机通过：App帖未发草稿→另一帖为空→返回原帖草稿/目标保留→测试草稿清空，未发送/投票/点反应。

证据：`20260908-forum-feature-current-posts.{json,log}`。正在补“发送回执先到、评论列表滞后”的保留逻辑回归；当前slice尚未commit/终验，真实发送及富文本嵌套完整矩阵仍待。

## 真实嵌套回复（2026-09-08 10:28）

70117回执早于GET新测试先红；增加只保留未被GET确认的成功echo，确认后按源列表正常刷新/删除，82770论坛相关测试和Debug/AndroidTest通过。当前开发APK`274371800940175E5EE3F41E8155082BAAEFF79211A5A568686F782A8C706121`。

第一次live初始化函数返回Boolean而非Unit，JUnit未开始执行，确认无pending/发送；77484只改测试返回值后重建。48212实际使用MainActivity/root已接feature：确认帖子1871及target2391均为本人，发一条`【Beta7原生嵌套回复验收-729d0bbc】仅验证回复的回复，请忽略。`，源GET确认恰好1条，createdReply2439/root4549。测试草稿清空。该测试回复留在本人帖下，未删除他人内容；不可重跑造重复测试评论。

证据`20260908-native-nested-reply-actual.{json,log}`及`20260908-forum-reply-live-report.json`。投票/点赞/踩/打赏没有在本次实测变更，仍需独立验证。
