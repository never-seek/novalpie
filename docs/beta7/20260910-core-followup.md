# 2026-09-10 核心后续验收

本轮维持原外观，不发布未验包，不触碰真实作品上传。

## 复现与修改

- 优化包章节10846528：首页仅标题，下一页才出现正文。分页`heading`信息贯穿测量与排版；只有标题占据当前页时允许正文按完整行分到此页。保留普通段落整段排版、超高行明确报错。回归验证所有offset/前后翻页无重复或漏字。
- 优化包系统TTS绑定约10秒而旧超时5秒。等待改为20秒、有上限且可取消。首次迟到初始化/停止/释放的真实Android生命周期仍需补验，不能把修改常量当作实际发声成功。
- 大上传章节JSON按当前网站5MiB阈值拆为50章一批，首次创建返回novelId后余批追加，MD5和批次字段保持协议。持久记录payload签名/已确认批/书ID/在途状态。每个POST前、确认回执后等待落盘；不明回执禁止自动重放，明确拒绝可以从已确认边界继续。

## 已执行

- 原实现JUnitCore 12项中标题/超时两红；修后54926编译成功，JUnitCore 14项全部通过。
- 54308标准Gradle定向16项：14绿、上传两红，确认旧版单POST与部分成功丢身份问题。
- 44564完成52项、3失败：新multipart测试未容纳标准Content-Length头；旧单次明确拒绝回执变成异常；解析期间表单修改被过度禁用。保持既有回执/编辑语义修实现，multipart测试改为读取字段而非假设头固定排列。新增批次持久恢复和真正TTS初始化/关闭测试，94975运行中。

## 后续不可遗漏

### 13:54 本轮闭环

67267最终build/lint成功14m41s，1152完整单测通过；非debug优化APK a7fdae0e...03fcf21/3,243,445bytes已实际install-r，冷启动1608ms，收藏65及继续阅读恢复。重新原生导出36021444章/6正文图+封面，6,166,238bytes，58条ZIP CRC与49个XML解析通过，260strong/204em，7图SHA与前包逐一相同。Librera打开同一段落，`20260910-styled-epub-librera-bold-confirmed.png`确认强调代码不再露出；不是仅结构测试。小书原文EPUB切片通过，不自动关闭原文/替换×TXT/EPUB全部最终矩阵或新1940未提供样本的失败反馈。

先核对SHA8f12ea9c...1b8ba40b0后，只删除这次生成的/sdcard/Download/说我是学院的恶劣CP厨理事长_360214_1789019361536.epub与host拉取副本。旧用户文件未动，通知原拒绝恢复、无活跃任务、MuMu正常shutdown。源码修复已本地提交f87d04d，未push；当前仍不发布。

### EPUB新缺陷修复中

39351“源强调不能露Markdown”红；复用Reader的applyMarkdownRanges，将源强调转换strong/em/del之后再对纯文本节点应用规则并XML转义，保持替换不能注入标签/图片。44463 28项仅测试未考虑&apos;的断言失败；修正断言。71949跨空行强调实际XML解析红，已改逐段闭合样式；下载package exportPipeline升4，旧完成包不冒充新排版。58067完整单测中，尚未生成/安装此最新片，待本书EPUB再次打开验证。

### 新优化包真实运行结果

133ec9ba无损覆盖安装，冷启动2003ms恢复原来阅读位置。书评进入的《说我是学院的恶劣CP厨理事长？》第1章10846528：标题+图片+正文同页截图`20260910-core-reader-title.png`已看；布局设定截图证实无限滚动关、翻页开、音量开、淡入，未改变这些原偏好。

系统eSpeak首次绑定约1.5秒，首次点击听书即PLAYING，音频帧824320→881664→1363968；暂停PAUSED、恢复/HOME背景PLAYING、回App点击停止后media_session清除。截图`20260910-core-tts-playing.png`包含真实正文高亮与暂停/停止。不是人工听辨音质或10秒冷引擎设备复验，12秒延迟由Robolectric生命周期测试覆盖。通知原拒绝已恢复，MuMu为后续构建shutdown，无运行播放/下载。

**新增阻断**：第2章首页音量上一页及左侧点击未回第1章末页；章内下一页/上一页正常，菜单上章可达。`...previous-chapter-tail.png`实际是第2章首页，文件名不能代替结果。36025正验证目录异步到达后NativePagedReader回调是否仍捕获旧hasPrevious；未确认修复前禁止发布。

08:19后续：36025 `catalogArrivingAfterBodyMustEnablePreviousChapterWithoutRecreatingReader`真实Compose红，确认局部函数引用`::move`在rememberUpdatedState中等值，目录变化不更新捕获的hasPrevious/hasNext。改普通捕获lambda，94413定向回归运行。该改动尚未生成/安装APK。

08:21 94413回归通过（含PagePlannerTest）1m38s；添加hasNext迟到对称用例，90389完整unit/optimizedBeta构建中。仍须新优化包跨章实测，不以测试代替运行。

- 03:30 30408完成：lint0errors/32warnings、优化打包成功；133ec9ba...e5c2eb4/3,243,449bytes已验签原Beta6证书与非debug属性。MuMu启动中，尚不能声称新包运行通过。

- 最新全量73255：159suites/1147tests全部绿；lint因本地缺31.7.3缓存未执行，30408正联网补回并构建优化包。不能把1147单测等同lint/R8/实机通过。
- 公开反馈20260910-delta：1940新报告“多本插图下载失败”，未提供书ID，0回复；已列入最终下载复验。App1871仍37条，无新用户回复。

- 94975共43项仅绑定关闭释放一红；已修`shutdown`无条件释放绑定连接、closed拒绝迟到OnInit/speak。上传确认边界和payload恢复定向通过。UI增加已确认批数/查看目标书/继续后批，未知批禁提交；新文件和已完成检查点隔离补回归。73255全量unit/lintDebug/assembleBeta在跑，尚无新包设备证据。

- 新文件清理旧批次、元数据冻结、已确认ID保存失败、真正进程中断恢复。
- 停止/关闭期间迟到TTS初始化不能继续播放或遗留引擎连接。
- 最新源码完整unit/lint、优化包构建验签、无损覆盖安装、新包实际首页/前后翻页/中文TTS/上传合成数据验证。
- 仍按96项需求与FINAL_PASS完成全业务/性能矩阵；旧包截图不替代当前包。
