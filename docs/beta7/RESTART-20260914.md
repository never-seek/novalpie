# Beta 7 重启记录（2026-09-14）

## 重启决定

用户确认：2026-09-13 08:30 之后的过程记录、阶段结论和“已验收”表述全部作废，不能继续作为 Beta 7 的证据。此前写入的源码、测试和产物也不自动视为正确；必须重新按需求、源码、当前 APK 和 MuMu 实机结果逐项审查。

## 新的证据规则

1. 以提交 `f87d04d5b677cbdc08a1c3c1e94ba490935aa887` 作为代码审计分界点；它之后的 dirty worktree 改动先视为待审，不直接合入结论。
2. 旧截图、旧测试数字、旧 APK、旧 Release 和旧网站反馈摘要全部只作线索，不作通过证据。
3. 每个模块重新建立：需求 → 当前实现 → 失败复现 → 回归测试 → 新候选包 → MuMu 操作 → 截图/日志 → 状态。
4. 不用“测试数量增加”“HTTP 200”“按钮存在”“以前修过”替代真实行为验证；写入操作不自动重发，不拿真实用户资产做未经标记的实验。
5. 先修阻断功能，再跑完整构建；未安装当前构建前，不把旧包结果迁给新包。

## 目前已确认的环境事实

- 当前分支仍有大量未提交改动；本次重启不直接删除它们，避免在没有可回滚快照时丢失代码。它们均标记为“待重新审计”。
- 重新启动前的最新 APK 不是本次重启后的验证包；后续必须重新构建、签名核验、覆盖安装和运行验收。
- MuMu 设备可能被断网/测试流程影响；每轮结束必须恢复网络、屏幕尺寸、字号、代理、通知权限和后台任务状态。
- 旧的测试 APK instrumentation 依赖曾出现 `androidx.tracing.Trace` 问题；重启后先单独验证测试驱动，不把测试驱动崩溃归因于生产 App。

## 重新执行顺序

1. 重新读取本会话需求和当前源码，生成按功能分区的验收表；旧 Beta7 文档只保留出处。
2. 阅读器：分页/反向跨章/无限滚动章评/工具栏/音量键/正文裁切/替换/TTS。
3. 下载：原文/替换 EPUB/TXT、文本无损压缩、原始图片字节、GIF/WebP、图片出现次数、暂停恢复和失败清理。
4. 论坛与书评：富文本、fold/黑幕/链接/书卡、回复的回复、反应数据和草稿。
5. 搜索/收藏/详情/个人/装扮/上传/消息/权限/网络逐模块验收。
6. 最后才生成 Beta 7 Release；源码、构建提交、APK、签名、安装包和 GitHub asset 必须同一份证据链。

## 本文件之后的有效记录

后续只认本文件之后新生成的测试、构建、截图和报告。任何旧文档中的“完成”“通过”“已发布”若没有关联新证据，都按未验收处理。
