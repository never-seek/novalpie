package com.novalpie.nativeapp.ui

/** Existing Compose call sites keep their API while the book feature owns mutable work. */
typealias BookEditState = com.novalpie.nativeapp.feature.books.BookManagementUiState
